name: CMake Build & Report

on:
  pull_request:
    types: [opened, synchronize, reopened]
  # 支持手动触发
  workflow_dispatch:
    inputs:
      pr_number:
        description: 'PR 编号（若想将结果评论到指定 PR，请输入数字；留空则仅编译不评论）'
        required: false
        default: ''

permissions:
  pull-requests: write
  contents: read

jobs:
  build-and-comment:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install ARM GCC Toolchain
        run: |
          sudo apt-get update
          sudo apt-get install -y gcc-arm-none-eabi libnewlib-arm-none-eabi build-essential

      - name: Configure CMake
        run: cmake -B build/Debug -DCMAKE_BUILD_TYPE=Debug -DCMAKE_TOOLCHAIN_FILE=cmake/gcc-arm-none-eabi.cmake

      - name: Build and Parse Logs
        run: |
          cmake --build build/Debug > build.log 2>&1 && echo "BUILD_STATUS=success" >> $GITHUB_ENV || echo "BUILD_STATUS=failure" >> $GITHUB_ENV
          
          WARNINGS=$(grep -c -i "warning:" build.log || echo 0)
          ERRORS=$(grep -c -i "error:" build.log || echo 0)
          
          echo "WARNINGS=$WARNINGS" >> $GITHUB_ENV
          echo "ERRORS=$ERRORS" >> $GITHUB_ENV

      - name: Comment on PR
        uses: actions/github-script@v7
        if: always()
        with:
          script: |
            // 兼容自动 PR 事件和手动输入的 PR 编号
            const prNumber = context.issue.number || context.payload.inputs?.pr_number;
            
            if (!prNumber) {
              console.log("提示：未检测到自动 PR 上下文，且未手动输入 PR 编号。将跳过评论步骤。");
              return;
            }
            
            const warnings = process.env.WARNINGS;
            const errors = process.env.ERRORS;
            const status = process.env.BUILD_STATUS;
            
            const isSuccess = status === "success";
            const statusEmoji = isSuccess ? "✅" : "❌";
            const statusText = isSuccess ? "编译通过" : "编译失败";
            
            const body = `### 🛠️ CMake 自动化编译报告 (Debug/ARM - 手动触发/更新)
            
            **编译状态**: ${statusEmoji} **${statusText}**
            
            | 统计项 | 数量 |
            | :--- | :--- |
            | 🔴 **Errors** | \`${errors}\` |
            | 🟡 **Warnings** | \`${warnings}\` |
            
            ${isSuccess ? '> 🎉 恭喜，固件编译成功！请检查并清理潜在的 Warning。' : '> ⚠️ **注意**: 编译未通过，请点击上面的 \`Details\` 查看具体报错日志并修复。'}
            `;
            
            await github.rest.issues.createComment({
              issue_number: parseInt(prNumber),
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: body
            });

      - name: Verify Build Success
        if: env.BUILD_STATUS == 'failure'
        run: |
          echo "编译失败，正在阻断当前环境..."
          exit 1
